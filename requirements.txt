Flask>=3.0
python-dotenv>=1.0
gunicorn>=21.2 ; sys_platform != "win32"

# Supabase Auth (GoTrue) and Resend are both plain HTTP APIs.
requests>=2.31

# Postgres. Identity lives in Supabase's auth schema, so this is required,
# not optional — there is no local fallback backend any more.
psycopg[binary]>=3.1
